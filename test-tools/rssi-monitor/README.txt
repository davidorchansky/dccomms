radio_rx_RSSI: programa de arduino para los Radiometrix que envia el RSSI continuamente por el puerto serie.
java-client: se conecta a un socket TCP desde donde recibe continuamente el RSSI.

para redirigir lo que envia la Arduino por el puerto serie a través de un socket TCP usar la tool "SerialLogger".
